'use client';
import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5eb898',
    },
    secondary: {
      main: '#a87909',
    },
    success: {
      main: '#04f149',
    },
  },
});

export default theme;
